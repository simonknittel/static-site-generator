version: 2.1

orbs:
  node: circleci/node@3.0.0

jobs:
  build_test:
    executor:
      name: node/default
      tag: '12'
    steps:
      - checkout
      - node/install-packages
      - run: npm test

workflows:
  build-test:
    jobs:
      - build_test
