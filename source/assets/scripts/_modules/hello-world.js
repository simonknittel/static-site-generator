/**
 * Greets the given name.
 * @method helloWorld
 * @param {string} name The name it should greet
 * @return {string} Returns the string with "Hello!" and the given name
 */
export default function helloWorld(name) {
    return `Hello ${name}!`;
}
